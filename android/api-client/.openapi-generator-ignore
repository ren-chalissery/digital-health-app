# The generator assumes it owns the whole build. It does not — this is one module of the Android
# project, so anything describing how to build it belongs to us and must survive regeneration.
#
# Verify by running `npm run generate:android` in api-contract and then building. Reading this file
# is not the check; the generator restoring a standalone build script is what breaks, and it breaks
# quietly.
build.gradle
settings.gradle
gradlew
gradlew.bat
gradle/**
proguard-rules.pro
README.md
docs/**
