package io.simplicity.training.model.enums;

/** Cross-organisation capability, held directly on the user. */
public enum PlatformRole {
  /** Operational access across every organisation. Held by the product team, not by customers. */
  SUPER_ADMIN,
  STANDARD
}
