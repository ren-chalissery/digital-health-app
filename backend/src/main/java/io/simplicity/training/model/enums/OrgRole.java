package io.simplicity.training.model.enums;

/** Role within a single organisation, held on the membership rather than the user. */
public enum OrgRole {
  /** Manages the organisation, its teams and invitations, and from Phase 2 its modules. */
  ORG_ADMIN,
  ORG_MEMBER
}
