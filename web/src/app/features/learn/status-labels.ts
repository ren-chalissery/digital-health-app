/**
 * Wording shared by Learn and the Dashboard. "Needs redoing" is deliberately plain: the clinician
 * did finish it, and the reason it is back is that the content changed substantively, not that
 * anything they did was wrong.
 */
export const STATUS_LABELS: Record<string, string> = {
  NOT_STARTED: 'Not started',
  IN_PROGRESS: 'In progress',
  COMPLETED: 'Complete',
  NEEDS_REDOING: 'Updated since you finished',
};
