# Pin the public registry. Without this, a developer whose user-level npmrc points at a private
# mirror bakes that hostname into package-lock.json, and CI then fails resolving a host it cannot
# reach.
registry=https://registry.npmjs.org/
